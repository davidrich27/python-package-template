name: Publish Conda to prefix.dev

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11']

    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          submodules: true

      - name: Prepare build environment
        run: |
          sudo apt-get update
          sudo apt-get install build-essential git git-lfs

      - name: Install conda
        run: |
          curl -L https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh > Miniconda3-latest-Linux-x86_64.sh
          bash Miniconda3-latest-Linux-x86_64.sh -b -p ~/miniconda
          ~/miniconda/bin/conda update -n base -c defaults conda

      - name: Build conda environment
        run: |
          source ~/miniconda/etc/profile.d/conda.sh
          conda config --prepend channels conda-forge
          conda config --prepend channels bioconda
          conda config --show channels
          conda env create -f environment-dev.yml
          conda activate package_name
          conda install -c conda-forge conda-build anaconda-client

      - name: Build package and upload to prefix.dev
        env:
          PREFIX_USER: ${{ vars.PREFIXDEV_USER }}
          PREFIX_TOKEN: ${{ secrets.PREFIXDEV_TOKEN }}
          PREFIX_FLAGS: ${{ vars.PREFIXDEV_FLAGS }}
          UPLOAD_TO_PREFIX: ${{ vars.UPLOAD_TO_PREFIXDEV }}
        run: |
          source ~/miniconda/etc/profile.d/conda.sh
          conda activate package_name
          if [[ ${UPLOAD_TO_ANACONDA_CLOUD} == true ]]; then
            conda build . --user ${ANACONDA_USER} --token ${ANACONDA_TOKEN} ${ANACONDA_FLAGS}
          else
            conda build .
          fi
